# URL base de la API. En desarrollo apunta al backend local (el proxy de Vite la ignora si dejas /api).
# En producción (GitHub Pages) define la URL del backend en AWS, ej: https://tu-backend.<region>.awsapprunner.com/api
VITE_API_URL=http://localhost:4000/api
